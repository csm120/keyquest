"""KeyQuest internal modules package.

This package contains the application's core modules. Most imports are explicit
(e.g., `from modules import state_manager`) rather than via `from modules import *`.
"""
